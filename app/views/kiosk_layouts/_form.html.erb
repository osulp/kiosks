<%= form_for(kiosk_layout) do |f| %>
  <% if kiosk_layout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kiosk_layout.errors.count, "error") %> prohibited this kiosk_layout from being saved:</h2>

      <ul>
      <% kiosk_layout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
